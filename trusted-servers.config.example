# public HTTP origin
# http://public-node.example:8022

# public HTTP origin | upstream base URL override
# http://public-node.example:8022|http://10.0.0.25:8080

# public HTTP origin | upstream base URL override | shared secret
# http://public-node.example:8022|http://10.0.0.25:8080|replace-with-a-long-random-secret
